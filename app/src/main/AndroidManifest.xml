<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" package="com.atv1calendarfsa">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_apptest"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_apptest_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalendarFSA"
        tools:targetApi="31">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PasswordRecoveryActivity" android:exported="false"/>
        <activity android:name=".DailyClassesActivity" android:exported="false"/>
        <activity android:name=".SemesterCoursesActivity" android:exported="false" />
        <activity android:name=".UserProfileActivity" android:exported="false" />
        <activity android:name=".RegistrationActivity" android:exported="false"/>


    </application>

</manifest>